# 待办与决策记录

## 已确认决策

- 主力开发放在虚拟机，鲁班猫用于阶段性实机验证和最终部署。
- 当前暂用 `640x480x15`，不继续追 `1280x720x30`，因为虚拟机下帧率不稳定。
- `offline_tray_debug` 的外框均分路线只作为历史基线，不再作为主要投入方向。
- 当前视觉几何主线转向 `tray_grid_debug`。
- 优先稳定 150 个穴位中心点，再做作物分类。
- 样本和 debug 输出不提交 Git，必要时只提交小型固定测试样例。

## 近期待办

1. 梳理未提交改动，拆成多个清楚提交。
2. 继续改进 `tray_grid_debug.py`。
3. 给每个苗盘的网格拟合输出质量评分。
4. 对 homography 增加更严格的成功条件：
   - 内点数量。
   - 行列覆盖数量。
   - 重投影误差。
   - 中心点是否落在图像和苗盘合理范围内。
5. 批量跑 10 组样本，生成 `_review` 总览。
6. 只有几何稳定后，再开始单穴分类。

## 暂缓事项

- 鲁班猫部署优化。
- 机械臂坐标转换和手眼标定细节。
- F407 真机通信联调。
- 作物分类模型训练。
- 高分辨率相机模式。

## 不建议继续投入的方向

- 只靠黑色阈值找外框。
- 只靠外框矩形机械均分 5 x 10。
- 为了让 debug 图“看起来有输出”而降低成功门槛。
- 在 `offline_tray_debug.py` 继续堆大量新算法。

