# 待办与决策记录

## 已确认决策

- 主力开发放在虚拟机，鲁班猫用于阶段性实机验证和最终部署。
- 当前暂用 `640x480x15`，不继续追 `1280x720x30`，因为虚拟机下帧率不稳定。
- 当前不要推进分类、矩阵美化、F407 联调，先解决视觉几何。
- 当前核心问题是“苗盘真实边界识别 + 透视矫正”。
- 样本原文件保留在 `samples/rgbd`，不要删除。
- 新 debug 输出放在 `samples/debug_/runs/<run_name>`。
- 历史 debug 输出移动到 `samples/debug_/archive`，不要混在当前查看目录。
- `samples/debug` 是旧历史目录，新实验不要再写进去。

## 近期待办

1. 整理未提交改动，拆成清楚提交。
2. 决定 `tray_rectify_debug.py` 是否作为实验脚本保留。
3. 冻结或清理 `offline_tray_debug.py` 中的实验堆叠。
4. 收敛边界识别算法：
   - 原图中找真实苗盘边缘。
   - 每条边拟合成直线。
   - 四条边相交得到四角。
   - 用四角做透视矫正。
5. 建立明确失败条件：
   - 边缘数量不足。
   - 四边形面积/长宽比异常。
   - 矫正后仍明显歪斜。
6. 只在几何稳定后，再恢复 5 x 10 网格、深度和分类。

## 暂缓事项

- 单穴 `0/1/2` 分类。
- 矩阵 overlay。
- 鲁班猫部署优化。
- 机械臂坐标转换和手眼标定。
- F407 真机通信联调。
- 作物分类模型训练。
- 高分辨率相机模式。

## 不建议继续投入的方向

- 只靠黑色阈值粗糙套矩形。
- 只靠外框矩形机械均分 5 x 10。
- 用孔中心拟合代替苗盘真实外框识别。
- 为了让 debug 图“看起来有输出”而降低成功门槛。
- 在 `offline_tray_debug.py` 继续堆大量新算法。
